# ASTRO backend — Python 3.12 / Django 4.2 LTS
Django>=4.2,<4.3
djangorestframework>=3.14,<3.16
gunicorn>=22,<24

# Auth (Google login via allauth)
django-allauth>=0.57,<0.62
PyJWT>=2.0
requests>=2.28
requests-oauthlib>=1.3
cryptography>=39

# API infra
django-cors-headers>=4.0
django-environ>=0.11
whitenoise>=6.5

# Google Calendar (read-only suggestions)
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1

# Exports
openpyxl>=3.1          # Excel (Budget, Plan)
reportlab>=4.0         # PDF / Gantt (Thai font embedding)

# Utils
python-dateutil>=2.8

# Production DB driver (dev uses SQLite; enabled via env)
psycopg2-binary>=2.9

# Tests
pytest>=7.4
pytest-django>=4.7
